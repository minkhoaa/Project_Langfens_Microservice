node_modules/
dist/*.tsbuildinfo
*.log
.DS_Store
# Parity check output (regenerated by `npm run parity`)
parity-results.json
# Package-level test suite (negate the project-root `tests` ignore so the
# vitest cases for this package ship with the source).
!tests/
!tests/*.ts
